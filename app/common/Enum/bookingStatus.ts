export enum StatusType {
  available = 'available',
  busy = 'busy',
  not_available = 'not_available',
  accepted = 'accepted',
  declined = 'declined',
  completed = 'completed',
  pending = 'pending',
  active = 'active',
  inactive = 'inactive',
  waiting = 'waiting',
}
